# Production builds use web/.env.production (Render API URL).
# Override here only if you need a different deployed API:
# VITE_API_BASE=https://thetalens-api.onrender.com

# Local dev: leave unset — Vite proxies /api and /health to localhost:8000
